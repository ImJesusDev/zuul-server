# Application name
spring.application.name=zuul-server

# Configuration Server
spring.profiles.active=dev
spring.cloud.config.uri=http://config-server-cluster-ip:8888

management.endpoints.web.exposure.include=*
